# mxpy governance get-config

Get the config of the governance contract.

## Usage

```bash
mxpy governance get-config [--proxy PROXY]
```

## Parameters

- `--proxy PROXY`  
  URL of the proxy.

## Example

```bash
mxpy governance get-config --proxy https://devnet-gateway.multiversx.com
