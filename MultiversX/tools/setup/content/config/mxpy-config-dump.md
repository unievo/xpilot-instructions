# mxpy config dump

Dumps the active configuration.

## Usage

```bash
mxpy config dump [--defaults]
```

## Parameters

- `--defaults`  
  Dump defaults instead of local config.

## Example

```bash
mxpy config dump
mxpy config dump --defaults
